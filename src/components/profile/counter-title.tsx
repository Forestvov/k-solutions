import styled from '@emotion/styled';

const Counter = styled.span`
    font-weight: 800;
    color: #006838;
    font-size: inherit;
    line-height: inherit;
    margin-left: 15px;
`;

export default Counter;
